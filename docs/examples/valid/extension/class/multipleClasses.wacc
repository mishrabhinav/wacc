{
  class A {
    int a;
    int b;

    void init(int x, int y) {
      @a = x ;
      @b = y
    }

    void a(int x) {
      @a = x
    }

    int a() {
      return @a
    }

    void foo() {
      println "bar"
    }
  }

  class B {
    int c;

    void init(int a) {
      @c = a
    }

    void c(int a) {
      @c = a
    }

    int c() {
      return @c
    }
  }

  A a = new A() ;
  B c = new B() ;

  call a->init(2, 3) ;
  call c->init(4) ;

  call a->foo() ;

  int y = call a->b() ;
  println y ;
  y = call c->c();
  println y ;

  call c->c(z + 1) ;

  y = call c->c() ;
  println y
}
